/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		remotePatterns: [
			{ hostname: "quirky-salmon-314.convex.cloud" },
			{ hostname: "quirky-salmon-314.convex.cloud" },
		],
	},
};

export default nextConfig;
